import React from 'react';
import { HeaderWrapper, HeaderTitle, HeaderSubTitle } from '../styled/Header';

const Header = () => {
    return (
        <HeaderWrapper>
            <HeaderTitle>Wesley College</HeaderTitle>
            <HeaderSubTitle>Learning Management System</HeaderSubTitle>
        </HeaderWrapper>
    );
};

export default Header;